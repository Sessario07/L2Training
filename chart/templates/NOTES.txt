l2lab {{ .Chart.Version }} installed as release "{{ .Release.Name }}" in namespace {{ .Release.Namespace }}.

  app       https://{{ include "l2lab.appHost" . }}
  grafana   https://{{ include "l2lab.grafanaHost" . }}

  api image       {{ include "l2lab.image" (list . .Values.app.image) }}
  frontend image  {{ include "l2lab.image" (list . .Values.frontend.image) }}
  vpc             {{ .Values.global.vpcId }}
  cluster         {{ .Values.global.clusterName }}

Grafana password (generated by Terraform, never stored in git):

  aws secretsmanager get-secret-value --region {{ .Values.global.region }} \
    --secret-id {{ include "l2lab.secretId" (list . "grafana") }} \
    --query SecretString --output text | jq -r '."admin-password"'

Check it came up:

  kubectl -n {{ include "l2lab.appNs" . }} get pods
  kubectl get ingress -A          # all three should share ONE ALB address
  kubectl -n {{ include "l2lab.appNs" . }} get endpointslices

{{ if not .Values.hardening.enabled -}}
Hardening is OFF. Turn it on only once the stack is green:

  --set hardening.enabled=true

It adds NetworkPolicy, ResourceQuotas and the backup CronJob. A subtly wrong
NetworkPolicy looks exactly like DNS failure or a dead dependency, so you want
a known-good baseline to compare against first.
{{- end }}

{{ if .Values.worker.workDelay -}}
WARNING: worker.workDelay is set to "{{ .Values.worker.workDelay }}".
The fan-out worker will lag deliberately. Unset it for normal operation.
{{- end }}
