apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Secrets first: the ServiceAccounts and the SecretProviderClass must exist
  # before any pod tries to mount them.
  - secrets
  - postgres
  - redis
  - nats
