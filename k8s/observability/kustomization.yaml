apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Order matters at apply time only loosely - Kubernetes reconciles - but
# listing storage backends before their collectors keeps the logs quieter
# on a cold start.
resources:
  - prometheus
  - alertmanager
  - loki
  - tempo
  - grafana
  - alloy
